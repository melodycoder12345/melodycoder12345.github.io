---
description: 博客项目 module、graph node、三级页面维护规范
alwaysApply: true
---

# Module / Node / Page 维护规范

新增或修改 module、graph node、三级页面时，必须遵守 `docs/module-node-spec.md`。

## 质量底线

- 不要只做能打开的最小页面。先思考学习路径、核心概念、边界情况、工程关联、图谱关系、交互完整性和验证方式。
- 只面向 PC 浏览器优化，不要为了移动端牺牲桌面布局和信息密度。
- 概念页默认读者第一次接触该主题，必须解释背景、术语、机制、误区和工程场景。

## 必须同步

- 新 module：同步目录、模块首页、`index.html`、`graph-data.js`、`graph.html`、`architecture.html`、`nav.js`、`footer.js`，必要时同步 `modules-map.md` 和 `docs/module-node-spec.md`。
- 新 graph node：使用稳定唯一 `id`、匹配的 `module`、`type: 'module' | 'page'`、相对 `href`、清晰 `desc`、有效 `GRAPH_EDGES`。
- 新增或修改三级页面：同步 `docs/module-node-spec.md` 的三级页面登记表。
- `graph.html` 和 `architecture.html` 必须提供有用的 hover / 选中文案，解释节点、模块或跨模块关系。
- 面包屑和星图来源返回逻辑应复用 `nav.js`；从星图打开的页面应返回星图。

## 页面类型

- A 类型：过程 / 算法 / 代码动画页，左侧文字，中间动画，右侧代码，参考成熟 `algo` 页面。
- B 类型：文字 / 代码 / 长文概念页，参考 `golang/type-system.html` 和其他 Golang 笔记。
- C 类型：概念 / 协议 / 系统流程动画页，详细文字加完整动画，代码区可选。

## 动画规则

- 参考 `algo` 模块动画风格和交互模型。
- 优先使用 step 数据驱动，提供播放、暂停、上一步、下一步、重置。
- 覆盖正常流程、边界情况、失败或特殊分支、最终状态。
- 视觉要清晰好看：布局稳定、标签可读、高亮有意义、步骤文案和画面同步。
- 不能用静态示意图替代必须实现的动画。
